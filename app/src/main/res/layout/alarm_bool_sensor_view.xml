<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:id="@+id/fullSensorView"
    >
    <hu.csanyzeg.android.homealone.UI.GraphBoolSensorView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/fullSensorGraphView"></hu.csanyzeg.android.homealone.UI.GraphBoolSensorView>
    <!--
    <RadioGroup
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">
        <RadioButton
            android:layout_width="match_parent"
            android:text="Riasztás, ha az érték igaz."
            android:layout_height="wrap_content"
            android:id="@+id/alarmTrue"></RadioButton>
        <RadioButton
            android:layout_width="match_parent"
            android:text="Riasztás, ha az érték hamis."
            android:layout_height="wrap_content"
            android:id="@+id/alarmFalse"></RadioButton>
        <RadioButton
            android:layout_width="match_parent"
            android:text="Nincs riasztás az érték alapján."
            android:layout_height="wrap_content"
            android:id="@+id/alarmNull"></RadioButton>
    </RadioGroup>
    -->
    <TextView
        android:layout_width="match_parent"
        android:textAlignment="center"
        android:layout_height="20dp"
        android:textSize="14dp"
        android:layout_marginTop="10dp"
        android:text="Riasztás mindenképp"
        android:id="@+id/alarmTextAlarm"/>
</LinearLayout>